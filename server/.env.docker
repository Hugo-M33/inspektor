# Docker-specific environment configuration
# This file is used when running the server in Docker Compose

# Ollama Configuration (points to Docker service)
OLLAMA_BASE_URL=http://ollama:11434

# Model Selection
# Development: llama3.2:1b (default in docker-compose.yml)
# Production: mistral:7b (default in docker-compose.prod.yml)
OLLAMA_MODEL=llama3.2:1b

# Server Configuration (bind to all interfaces in container)
HOST=0.0.0.0
PORT=8000
